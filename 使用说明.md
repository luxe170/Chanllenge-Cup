# 职涯棱镜 — 前后端使用说明

> 快速上手文档。按本文档步骤即可把系统跑起来，并体验已完成的「岗位演化」功能。

---

## 1. 这是什么

「职涯棱镜」是一套多源异构数据驱动的岗位能力图谱与动态演化分析系统（挑战杯，赛题编号 XH-202621）。

它采集多家企业的招聘 JD，抽取「岗位—技能」关系，分析岗位能力随时间的演化，并自动发现增长中的新岗位。

**当前已完成并接通真实数据的部分**：岗位演化页（能力变更记录 + 新岗位发现，含原始证据查看与岗位定义查看）。

---

## 2. 运行环境要求

| 依赖    | 版本要求                     | 说明         |
| ------- | ---------------------------- | ------------ |
| Python  | 3.10 及以上（开发机为 3.14） | 运行后端     |
| Node.js | 18 及以上（开发机为 24）     | 运行前端     |
| npm     | 随 Node 自带                 | 安装前端依赖 |
| 浏览器  | Chrome / Edge 等现代浏览器   | 访问页面     |

> 后端需要连接外网抓数据的话才需要 Playwright；**仅跑本系统演示不需要联网**，数据已内置。

---

## 3. 目录结构（关键部分）

```text
挑战杯/
├── backend/                      # FastAPI 后端（岗位演化接口）
│   └── app/
│       ├── main.py               # 接口入口
│       ├── schemas.py            # 数据结构定义
│       └── services/
│           └── evolution_service.py   # 核心计算逻辑
├── frontend/                     # React + TypeScript 前端
│   └── src/
│       ├── pages/                # 各页面
│       ├── services/api.ts       # 请求后端的地址（写死 http://127.0.0.1:8000）
│       └── components/           # 组件（含 Modal）
├── data/
│   └── processed/
│       └── relevant_jobs.jsonl   # ★ 关键数据：清洗后的真实 JD（约 1.2MB，必须随包分发）
├── src/                          # 爬虫与清洗脚本（可选）
├── docs/                         # 产品/接口/数据文档
├── tests/                        # 自动化测试
├── requirements.txt              # Python 依赖
└── 使用说明.md                   # 本文档
```

---

## 4. 快速启动（三步）

### 第一步：安装依赖

**后端**（在项目根目录，即包含 `backend/` 的目录）：

```powershell
python -m venv .venv
.venv\Scripts\activate          # macOS / Linux 用： source .venv/bin/activate
pip install -r requirements.txt
```

> 说明：虚拟环境 `.venv` 不可跨机器复制（里面记录了绝对路径），队友需按上面步骤**重新创建**。

**前端**：

```powershell
cd frontend
npm install
```

### 第二步：启动后端

在项目根目录（含 `backend/` 的目录），保持虚拟环境已激活：

```powershell
uvicorn backend.app.main:app --reload --port 8000
```

看到下面这行说明启动成功：

```text
Uvicorn running on http://127.0.0.1:8000
```

可自测一下（应返回 `{"status":"ok"}`）：

```powershell
curl http://127.0.0.1:8000/health
```

### 第三步：启动前端

另开一个终端：

```powershell
cd frontend
npm run dev
```

看到 `Local: http://localhost:5173/` 后，用浏览器打开 **http://localhost:5173** 即可。

> 前后端都要开着。前端固定用 5173 端口，后端固定用 8000 端口（前端代码里写死了 8000，改端口会连不上）。

---

## 5. 功能使用说明

左侧导航共 6 个页面。**当前只有「岗位演化」接入了真实后端数据**，其余 5 个页面仍是本地演示数据（Mock），可浏览界面但数据是写死的。

### 5.1 岗位演化（本次交付的核心功能）

进入左侧「岗位演化」页，顶部有两个标签，右侧有搜索框（按岗位名 / 技能名过滤）。

#### 标签一：能力变更记录

1. 左侧列表展示每条「岗位—技能」的能力变化，包含变化类型（新增 / 上升 / 下降）、变化前后状态、证据条数、可信度。
2. **点击任意一行**，右侧会展示这条变化的「变化证据链」：
   - 综合可信度
   - 多源数据支持（来自几家企业、几条有效 JD）
   - 跨窗口持续出现（连续几个时间窗口达标）
   - 语义一致性校验
3. 点右下角 **「查看原始证据」** 按钮 → 弹出弹窗，展示这条变化对应的**真实 JD 原文**：
   - 可切换多条 JD（若有多条证据）
   - 含企业、岗位名称、来源平台、采集时间、命中技能、来源链接
   - 正文为完整 JD 原文，可滚动阅读

#### 标签二：新岗位发现

1. 卡片式展示候选新岗位，每张卡片含增长率、可信度、首次出现时间、数据源数量、技能标签。
2. 点卡片底部 **「查看岗位定义」** → 弹出弹窗，展示该岗位的**完整岗位定义**：
   - 岗位描述、岗位别名、所属类别
   - 核心职责、应用场景
   - 必备技能 / 加分技能（含出现频次与可信度）
   - 支撑样本数、首次 / 最近出现时间

### 5.2 其他页面（Mock 演示，无需后端）

| 页面       | 说明                  | 数据状态 |
| ---------- | --------------------- | -------- |
| 全局工作台 | 数据概览、评价指标    | Mock     |
| 岗位图谱   | 三维知识图谱（WebGL） | Mock     |
| 简历解析   | 简历技能抽取          | Mock     |
| 匹配诊断   | 人岗匹配与差距分析    | Mock     |
| 审核评测   | 待确认岗位/变更审核   | Mock     |

> 这些页面对应的后端接口（`/dashboard`、`/graph`、`/reviews` 等）尚未实现，后续迭代会逐步接通。

---

## 6. 数据说明与更新方法

### 6.1 数据来源

页面上的岗位、技能、可信度、新岗位发现，全部由后端**实时计算**自：

```text
data/processed/relevant_jobs.jsonl
```

这个文件是清洗后的真实 JD（每行一条 JSON），后端启动后每次请求都会重新读它并计算。**没有数据库，也没有"手动新增岗位"的操作**——换数据文件，结果就会变。

### 6.2 如何更新数据（可选，演示通常不需要）

完整流程：

```powershell
# 1. 爬取 JD（需要联网 + Playwright）
python src/crawlers/multi_company_jobs_spider.py --max-pages 1

# 2. 清洗
python src/processing/clean_multisource_jobs.py

# 3. 重启后端即可生效
```

清洗脚本会把结果写入 `data/processed/`，覆盖 `relevant_jobs.jsonl` 后重启后端即可看到新数据。

### 6.3 可信度是怎么算的（简要）

- **能力变化可信度** = 0.35×来源广度 + 0.25×证据量 + 0.20×持续性 + 0.20×语义一致性（各项先归一化到 0~1）。
- **新岗位可信度** = min(0.99, 0.55 + 增长率×0.35)。
- 判定"新岗位"的简化规则：样本 JD ≥3 条，且相对 2025 年前 JD 数量增长 >10%。

具体公式和阈值都在 `backend/app/services/evolution_service.py` 里，需要调整"手感"时改那里。

---

## 8. 常见问题排查

| 现象                                        | 原因与解决                                                                                                     |
| ------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| 打开页面后「岗位演化」是空的 / 报红         | 后端没启动或 8000 端口被占。确认后端终端有 `Uvicorn running`，并 `curl http://127.0.0.1:8000/health` 能通  |
| 浏览器控制台报 `Failed to fetch`          | 后端没开，或前端连不上 8000。确认后端已启动                                                                    |
| 「查看原始证据」/「查看岗位定义」点了没反应 | 数据文件缺失（`data/processed/relevant_jobs.jsonl`），或后端 404。看浏览器 F12 → Network 里对应请求的状态码 |
| 端口 5173 被占用                            | 改 `frontend/vite.config.ts` 里的 `server.port`（同时后端地址不变）                                        |
| 后端启动报 `ModuleNotFoundError: backend` | 启动命令要在项目根目录（含 `backend/` 的目录）执行，不能用别的路径                                           |
| Python 依赖安装慢/失败                      | 仅演示只需 fastapi/uvicorn/pydantic，可单独安装这三个，不必装 playwright                                       |
